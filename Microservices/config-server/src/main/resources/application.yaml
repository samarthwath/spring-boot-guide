spring:
  application:
    name: config-server
  profiles:
    active: native
server:
  port: 8088
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: "http://localhost:8761/eureka/"