spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/SMART_HEALTHCARE
    username: ${MYSQL_USER:laravel}
    password: ${MYSQL_PASSWORD:laravel}
  application:
    name: doctor-service
  jpa:
    hibernate:
      ddl-auto: update